package com.koala.emm.basicdata;

import com.lidroid.xutils.util.LogUtils;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.os.Handler;
import android.os.Message;

public class EMMReceiver extends BroadcastReceiver{

	@Override
	public void onReceive(Context context, Intent intent) {
		LogUtils.e("MDM外部广播接受者");
//		if(BasicDataService.getInstance() == null){
//			final Context c = context;
//			final Intent i = intent;
//			
//			LogUtils.e("MDM外部广播接受者 -->开启服务");
//			Intent service = new Intent(context,BasicDataService.class); 
//			service.putExtra("firstStart", true);
//            context.startService(service);
//            new Handler(){
//            	@Override
//            	public void handleMessage(Message msg) {
//            		BasicDataService.getInstance().handMDM(c, i);
//            	}
//            }.sendEmptyMessageDelayed(0, 2000);
//            
//		}else{
//			BasicDataService.getInstance().handMDM(context, intent);
//		}
		
	}

}
