package com.koala.emm.util;

import android.content.Context;

/**
 * @author hanrong
 * @version 创建时间：2014-12-25 上午10:45:14 类说明
 */
public class UserInfoUtils {
	private Context context = null;
	// private UserDao mUserDao = null;

	private String userName = "";
	private String userDelvorgCode = "";
	private String userPWD = "";
	private String dlvsectionid = "";
	private String dlvsectionCode = "";
	private String sid = "";
	private String dlvflag = "";
	private String realname = "";
	private String mobile = "";
	// private UserInfoBean bean = null;
	private String postcode = null;

	public UserInfoUtils(Context context) {
		if (context != null) {
			this.context = context;
			// userInfo();
		}
	}

	// public synchronized void userInfo() {
	// mUserDao = DeliverDaoFactory.getInstance().getUserDao(context);
	// bean = mUserDao.getUserInfo();
	// try {
	// if (bean.getLub().getUsername() != null
	// && !"".equals(bean.getLub().getUsername())) {
	// userName = bean.getLub().getUsername();
	// // Log.e("tag", userName);
	// }
	// if (bean.getLub().getDelvorgcode() != null
	// && !"".equals(bean.getLub().getDelvorgcode())) {
	// userDelvorgCode = bean.getLub().getDelvorgcode();
	// // Log.e("tag", userDelvorgCode);
	// }
	// if (bean.getLub().getDlvsectionid() != null
	// && !"".equals(bean.getLub().getDlvsectionid())) {
	// dlvsectionid = bean.getLub().getDlvsectionid();
	// // Log.e("tag", dlvsectionid);
	// }
	// if (bean.getLub().getSid() != null
	// && !"".equals(bean.getLub().getSid())) {
	// sid = bean.getLub().getSid();
	// // Log.e("tag", sid);
	// }
	// if (bean.getLub().getDlvflag() != null
	// && !"".equals(bean.getLub().getDlvflag())) {
	// dlvflag = bean.getLub().getDlvflag();
	// // Log.e("tag", dlvflag);
	// }
	// if (bean.getLub().getRealname() != null
	// && !"".equals(bean.getLub().getRealname())) {
	// realname = bean.getLub().getRealname();
	// // Log.e("tag", realname);
	// }
	// if (bean.getLub().getMobile() != null
	// && !"".equals(bean.getLub().getMobile())) {
	// mobile = bean.getLub().getMobile();
	// // Log.e("tag", mobile);
	// }
	// if (bean.getLub().getPostcode() != null
	// && !"".equals(bean.getLub().getPostcode())) {
	// postcode = bean.getLub().getPostcode();
	// // Log.e("tag", mobile);
	// }
	// if (bean.getLdb().getDlvsectioncode() != null
	// && !"".equals(bean.getLdb().getDlvsectioncode())) {
	// dlvsectionCode = bean.getLdb().getDlvsectioncode();
	// }
	//
	// } catch (Exception e) {
	// }
	//
	// }

	public String getDlvsectionCode() {
		return dlvsectionCode;
	}

	public String getUserName() {
		return userName;
	}

	public String getUserDelvorgCode() {
		return userDelvorgCode;
	}

	public String getUserPWD() {
		return userPWD;
	}

	public String getSid() {
		return sid;
	}

	public String getDlvflag() {
		return dlvflag;
	}

	public String getRealname() {
		return realname;
	}

	public String getMobile() {
		return mobile;
	}

	public String getDlvsectionid() {
		return dlvsectionid;
	}

	public String getPostcode() {
		return postcode;
	}

	public void setPostcode(String postcode) {
		this.postcode = postcode;
	}
}
